FROM python:3.11-slim

RUN pip install --no-cache-dir "numpy>=1.24,<2.0" "scipy>=1.10,<2.0"

WORKDIR /app
COPY . .

ENV PYTHONPATH=/app

ENTRYPOINT ["python", "Dinamica/servicio_dinamica.py"]
CMD ["--fuente", "BESS", "--SoC", "0.5", "--puerto", "6000"]
